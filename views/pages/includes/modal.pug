#modalPostList.modal.fade(tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true")
  .modal-dialog.modal-dialog-centered
   .modal-content
      .modal-header
        h5#exampleModalLabel.modal-title Título
        button.close(type="button" data-dismiss="modal" aria-label="Close")
          span(aria-hidden="true") &times;
      .modal-body
        p Cuerpo del modal
      .modal-footer.border-0
        button.btn.btn-secondary(type="button" data-dismiss="modal") Cancelar
        a.cta.btn.btn-info(href="#") Aceptar